# Built-a-Python-Email-Validator-Processor-and-it-logs-everything.

Receives email input from the user
Validates it using regex
Logs errors to a file if the email is invalid
Cleans & structures valid emails (lowercased, trimmed, domain extracted)
Logs every single step using Python's logging module
